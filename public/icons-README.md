# 카드 혜택 모아보기 PWA — 아이콘 자리

`vite.config.ts` manifest가 아래 파일을 참조한다. 아직 실 PNG가 없으므로
PWA 설치 아이콘은 2단계에서 추가한다.

- [ ] `public/pwa-192x192.png` (192x192)
- [ ] `public/pwa-512x512.png` (512x512, maskable 겸용)

임시로 `public/favicon.svg`가 파비콘 역할을 한다.
아이콘 추가 후 `includeAssets`에 이미 포함되어 있어 별도 설정 변경은 불필요하다.
